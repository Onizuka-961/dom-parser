{
  "manifest_version": 3,
  "name": "DOM Extractor",
  "description": "Extracts the full DOM from the active page.",
  "version": "1.0.0",
  "permissions": ["activeTab", "scripting", "tabs"],
  "action": {
    "default_popup": "popup.html"
  }
}
